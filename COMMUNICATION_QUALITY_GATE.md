# Communication Quality Gate

## 目标

本文定义通信功能从设计、bring-up、联调、回归、量产到发布的质量门禁，用于判断一个通信链路是否可以进入下一阶段或对外发布。

质量门禁解决的问题：

- 避免“能通一次”被误认为“可发布”。
- 把协议风险转成明确证据和测试要求。
- 把设计评审、测试矩阵、自动化治具和可观测性串起来。
- 让现场故障修复必须进入回归和监控闭环。
- 为高风险协议设置更高发布标准。

## 门禁阶段

建议分为七个阶段：

| 阶段 | 目标 | 输出 |
| --- | --- | --- |
| G0 Requirement | 明确需求和风险 | 协议选型、需求表、风险分级 |
| G1 Design Review | 设计可评审 | 原理图、接口参数、状态机、测试点 |
| G2 Bring-up | 最小通信可用 | ID、枚举、Link、基础事务 |
| G3 Integration | 协议事务完整 | 正常流程、错误路径、日志 |
| G4 Regression | 版本回归通过 | 自动化结果、抓包、计数器 |
| G5 Production Ready | 可量产测试 | 工位测试、序列号、追溯日志 |
| G6 Release | 可发布和维护 | 发布报告、已知问题、回滚方案 |

任何阶段失败都应记录 blocker、owner 和关闭条件。

## 风险分级

按影响和复杂度分级：

| 等级 | 典型协议 | 门禁强度 |
| --- | --- | --- |
| R1 低风险 | GPIO、UART 日志、简单 I2C 传感器 | 最小通信和基本错误处理 |
| R2 中风险 | SPI Flash、PMBus、Modbus、USB CDC | 参数边界、恢复和量产测试 |
| R3 高风险 | CANopen、UDS、Ethernet、USB DFU、MIPI | 错误注入、长稳、可观测性和回归矩阵 |
| R4 关键风险 | 固件升级、制动/运动控制、车载视频、TSN、PCIe DMA | 断电恢复、失效安全、自动化回归、发布评审 |

风险等级不是固定协议属性，要结合产品场景判断。USB CDC 调试口可能是 R1，USB DFU 量产升级可能是 R4。

## G0 需求门禁

必须回答：

- 通信距离、速率、实时性和设备数量。
- 是否有安全、认证、加密或诊断要求。
- 是否需要热插拔、断电恢复或远程维护。
- 是否会影响安全动作、升级、量产或售后。
- 是否有客户生态约束，如 PLC、DBC、ODX、EDS、GSDML。

通过条件：

- 已完成协议选型。
- 已明确风险等级。
- 已列出必须测试的失败场景。
- 已明确调试工具和测试治具可获得。

证据：

- `PROTOCOL_SELECTION.md` 对应结论。
- 需求表或接口规格。
- 风险清单。

## G1 设计评审门禁

硬件必须确认：

- 电源和 IO 电平。
- 终端、上拉、偏置、隔离和保护。
- 高速差分阻抗和参考平面。
- 连接器 pinout 和线缆定义。
- 测试点和调试入口。

软件必须确认：

- 初始化顺序。
- 状态机。
- 超时、重试和恢复。
- 错误码和日志字段。
- 配置和版本兼容策略。

通过条件：

- 评审问题关闭或有风险接受。
- 高风险链路有可观测性字段设计。
- 已定义最小 bring-up 用例。

证据：

- `REVIEW_CHECKLIST.md` 评审记录。
- 原理图检查结果。
- 驱动状态机草图。
- 测试计划。

## G2 Bring-up 门禁

目标是证明最小链路成立。

不同协议最小证据：

| 协议族 | 最小证据 |
| --- | --- |
| I2C/SMBus/PMBus | 地址应答、ID 或 STATUS_WORD、波形边沿 |
| SPI/QSPI | JEDEC ID、读写校验、CS/SCK/MISO/MOSI trace |
| UART/RS485 | 固定帧收发、方向控制、CRC |
| CAN | 两节点收发、ACK、错误计数为 0 |
| USB | 枚举、描述符、Endpoint 可用 |
| Ethernet | PHY Link、ARP、ping、MAC 唯一 |
| MIPI/SerDes | Sensor ID、Link Lock、CSI/DSI 基础状态 |
| PCIe/SDIO | 枚举、ID、Link Width/Speed 或 bus-width |

通过条件：

- 最小通信稳定重复 10 次以上。
- 失败时能看到明确错误层级。
- 测量证据已归档。

证据：

- `MEASUREMENT_EXAMPLES.md` 对应样例。
- 波形、抓包、日志或寄存器 dump。
- bring-up 测试记录。

## G3 集成门禁

目标是验证完整协议事务和错误路径。

必须覆盖：

- 正常事务。
- 参数边界。
- 错误输入。
- 超时。
- 断线或设备离线。
- 恢复动作。
- 应用层状态限制。

通过条件：

- 协议事务完整通过。
- 常见错误路径返回明确错误码或状态。
- 驱动状态能从错误恢复到 READY/RUNNING。
- 日志和计数器能解释失败。

证据：

- 协议 trace。
- 应用日志。
- 错误计数器。
- 自动化测试结果。

## G4 回归门禁

目标是防止版本迭代引入回归。

必须覆盖：

- `REGRESSION_TEST_MATRIX.md` 中对应协议的核心用例。
- 近期修复过的故障案例。
- 高风险异常注入。
- 长时间稳定性。
- 测试 artifact 归档。

通过条件：

- must-pass 用例全部通过。
- 失败用例有明确 owner、风险接受或修复计划。
- 新增故障修复已加入回归。
- p99/p999、错误计数或恢复次数没有超阈值。

证据：

- 自动化测试 summary。
- 原始 artifact 链接。
- 趋势对比。
- 已知问题列表。

## G5 量产门禁

目标是确保生产阶段能检测关键连接和功能问题。

必须覆盖：

- 电源和基础连通性。
- 每个外部接口最小通信。
- 唯一 ID、MAC、序列号或校准数据写入。
- 关键外设 ID 读取。
- 失败日志和工位追溯。

通过条件：

- 量产测试项可自动执行。
- 每台设备绑定测试结果。
- 工位自身有自检。
- 失败项能定位到硬件、软件、治具或外设。

证据：

- `PRODUCTION_TEST.md` 测试策略。
- 工位测试报告。
- 治具版本和校验记录。
- 抽检波形或抓包。

## G6 发布门禁

发布前必须确认：

- 所有 G4 must-pass 用例通过。
- G5 量产测试覆盖关键接口。
- 高风险协议有恢复策略和远程诊断能力。
- 固件升级链路有防变砖和回滚策略。
- 现场日志能导出。
- 已知问题有规避措施和影响评估。
- 回滚版本和升级路径已验证。

发布报告建议包含：

```text
版本号
硬件版本范围
协议和接口变更
测试范围
未覆盖范围
失败和风险接受
现场观测项
回滚方案
```

## 门禁决策表

| 结果 | 含义 | 动作 |
| --- | --- | --- |
| Pass | 满足门禁 | 进入下一阶段 |
| Conditional Pass | 存在低风险问题 | 记录风险和 owner |
| Blocked | 缺少证据或环境 | 补测试或补工具 |
| Fail | 核心用例失败 | 修复后重新门禁 |

`Conditional Pass` 不能用于 R4 关键风险的安全、升级和失效恢复用例。

## 质量指标

建议持续跟踪：

- Bring-up 首次通过率。
- 自动化回归通过率。
- flaky 用例数量。
- 长稳错误计数增长率。
- 现场通信问题复发率。
- 故障从发现到加入回归的时间。
- 量产通信类不良率。
- 升级失败率和回滚成功率。

指标趋势比单次结果更重要。

## 与其他文档的关系

- `PROTOCOL_SELECTION.md`：决定 G0 的协议和风险。
- `REVIEW_CHECKLIST.md`：支撑 G1 设计评审。
- `MEASUREMENT_EXAMPLES.md`：支撑 G2/G3 证据采集。
- `REGRESSION_TEST_MATRIX.md`：支撑 G4 回归范围。
- `AUTOMATED_TEST_HARNESS.md`：支撑 G4 自动执行。
- `PRODUCTION_TEST.md`：支撑 G5 量产测试。
- `COMMUNICATION_OBSERVABILITY.md`：支撑 G3 到 G6 的日志、状态和远程诊断。
- `FAILURE_CASES.md`：把现场问题反向沉淀到门禁和回归。

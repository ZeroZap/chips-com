# Communication Security And Safety

## 目标

本文整理通信链路的安全与失效安全设计方法，覆盖威胁建模、权限控制、诊断安全、固件升级安全、日志审计、降级策略和安全门禁。

这里的安全包括两类：

```text
Security：防止未授权访问、篡改、重放、降级和数据泄露
Safety：通信失败或异常输入时系统仍进入可控安全状态
```

两者经常交叉。例如固件升级既要防止伪造镜像，也要防止断电变砖。

## 威胁建模

先识别攻击面：

| 攻击面 | 示例 |
| --- | --- |
| 物理接口 | UART 调试口、JTAG/SWD、USB、CAN、Ethernet |
| 管理总线 | I2C/SMBus/PMBus 配置写入 |
| 诊断接口 | UDS、DoIP、厂商私有命令 |
| 网络接口 | MQTT、HTTP、TCP/UDP、工业以太网 |
| 升级链路 | USB DFU、UDS 刷写、OTA、产线烧录 |
| 测试治具 | 量产工具、脚本、密钥、配置文件 |
| 日志导出 | 现场诊断包、抓包、dump、序列号 |

威胁类型：

- 未授权读取。
- 未授权写入。
- 命令重放。
- 参数篡改。
- 固件降级。
- 错误镜像刷写。
- 敏感日志泄露。
- 诊断接口被滥用。
- 拒绝服务。
- 失效后进入危险状态。

## 风险分级

| 等级 | 示例 | 要求 |
| --- | --- | --- |
| S1 | 调试日志、只读状态 | 基础访问控制和日志脱敏 |
| S2 | 配置写入、校准参数 | 权限、范围检查、审计 |
| S3 | 诊断例程、DTC 清除、远程控制 | 会话、安全等级、操作条件 |
| S4 | 固件升级、运动控制、安全相关输出 | 认证、完整性、回滚、失效安全 |

风险等级应进入 `COMMUNICATION_QUALITY_GATE.md` 和 `COMMUNICATION_TRACEABILITY.md`。

## 最小安全原则

通信安全设计遵循：

- 默认拒绝。
- 最小权限。
- 敏感服务显式授权。
- 所有写操作做范围检查。
- 所有升级做完整性校验。
- 所有关键操作有审计日志。
- 失败进入安全状态。
- 不在日志中保存明文密钥。
- 不依赖隐藏命令作为唯一保护。

## 访问控制

按操作分级：

| 操作 | 控制要求 |
| --- | --- |
| 读取版本和状态 | 通常可开放，但注意隐私 |
| 读取诊断数据 | 按场景限制范围 |
| 写配置 | 需要认证、范围检查和审计 |
| 清故障 | 需要会话、权限和条件检查 |
| 执行例程 | 需要权限、互锁和超时 |
| 固件升级 | 需要认证、完整性、版本策略和回退 |
| 控制执行器 | 需要安全状态、互锁和失效安全 |

权限不要只放在 UI 或上位机。设备端必须再次检查。

## 诊断安全

UDS、DoIP、厂商诊断和远程维护要关注：

- 会话控制。
- Security Access。
- 尝试次数限制。
- 延时和锁定策略。
- 敏感 DID/Routine 权限。
- DTC 清除条件。
- 刷写前置条件。
- Tester Present 滥用。
- 网关路由权限。
- 远程诊断审计。

UDS SecurityAccess 要记录：

```text
seed_requested
key_attempt_count
security_level
delay_active
exceeded_attempts
last_security_error
```

不要把所有权限失败都返回通用错误。应能区分会话错误、安全等级不足、条件不满足和请求顺序错误。

## 固件升级安全

升级链路必须防止：

- 错硬件镜像。
- 镜像被篡改。
- 镜像不完整。
- 版本回滚，视安全需求。
- 断电变砖。
- Bootloader 被覆盖。
- metadata 半写。
- 应用启动失败后无法恢复。

镜像 header 建议包含：

```text
magic
header_version
hardware_id
firmware_version
image_size
load_address
entry_point
crc_or_hash
signature
rollback_counter
```

Bootloader 必须检查：

- header magic。
- 硬件兼容性。
- 地址范围。
- 向量表或入口地址。
- 镜像长度。
- CRC/hash。
- 签名，视产品要求。
- 防回滚版本。

升级状态必须可恢复：

```text
IDLE
DOWNLOADING
VERIFYING
READY_TO_BOOT
BOOT_PENDING_CONFIRM
CONFIRMED
FAILED
ROLLBACK
```

不要在未校验完整镜像前覆盖唯一可启动应用。

## 重放和降级

重放风险常见于：

- 诊断解锁。
- 参数写入。
- 远程控制。
- MQTT 命令。
- 固件升级。

缓解措施：

- nonce 或随机挑战。
- 单调计数器。
- 时间戳和有效期。
- 版本防回滚。
- 命令序号。
- 会话绑定。
- 审计日志。

如果设备没有可靠时间，不能单独依赖时间戳。

## 数据完整性

不同层级使用不同机制：

| 层级 | 机制 |
| --- | --- |
| 帧级 | CRC、PEC、checksum |
| 传输级 | 序号、长度、超时、重组校验 |
| 应用级 | 参数范围、单位、字节序、对象权限 |
| 镜像级 | hash、签名、版本、硬件匹配 |
| 存储级 | 双备份、写后读、掉电恢复 |

不要把链路 CRC 当成应用安全校验。

## 失效安全

通信失败时系统必须进入可控状态。

典型策略：

| 场景 | 失效安全动作 |
| --- | --- |
| 电机控制心跳丢失 | 降速、停机或进入 Fault |
| 传感器数据超时 | 使用无效标志，不使用旧值当真值 |
| PMBus 遥测异常 | 告警但不立即断电，先看 status bits 和持续时间 |
| SerDes 摄像头掉线 | 标记通道无效，保留其他通道，记录 lock drop |
| TSN 时间同步丢失 | 降级实时流或停止依赖确定性的动作 |
| 固件升级失败 | 回退 Bootloader 或旧镜像 |
| 远程诊断异常 | 关闭敏感会话，保留只读状态 |

失效安全动作要能被测试和审计。

## 安全日志

安全相关事件必须记录：

- 权限失败。
- 安全访问失败。
- 敏感 DID/Routine 调用。
- 配置写入。
- 固件升级开始、完成、失败。
- 镜像校验失败。
- 防回滚拒绝。
- 远程登录或远程诊断。
- 关键输出进入安全状态。

日志字段：

```text
time
interface
source
operation
security_level
result
reason
version
counter
```

不要记录明文 key、token、私钥或完整敏感 payload。

## 安全测试矩阵

| 测试 | 目的 | 证据 |
| --- | --- | --- |
| 未授权写配置 | 验证默认拒绝 | NRC/错误码、审计日志 |
| 错 key 多次 | 验证尝试次数和延时 | security counter、delay state |
| 重放旧命令 | 验证 nonce/序号 | 拒绝日志 |
| 错硬件镜像 | 验证硬件匹配 | DFU/Bootloader 日志 |
| 篡改镜像 | 验证 hash/签名 | 校验失败记录 |
| 低版本镜像 | 验证防回滚 | rollback reject |
| 升级中断电 | 验证防变砖 | metadata、回退结果 |
| 通信断线 | 验证失效安全 | 状态机、输出状态 |
| 日志导出 | 验证脱敏 | 导出包检查 |

这些测试应进入 `REGRESSION_TEST_MATRIX.md` 的 R3/R4 用例。

## 安全门禁

R3/R4 通信功能发布前必须确认：

- 敏感服务有设备端权限检查。
- 写操作有范围检查和审计。
- 固件镜像有完整性校验。
- Bootloader 或恢复入口受保护。
- 失败能进入安全状态。
- 安全事件能导出日志。
- 风险接受有 owner 和有效期。
- 安全相关故障有回归用例。

任何影响固件升级、运动控制、远程诊断、制动、车载视频或生产配置的通信变更，都不应跳过安全门禁。

## 现场安全事件处理

现场发现疑似安全事件时：

1. 保护现场日志和诊断包。
2. 记录版本、序列号、接口和来源。
3. 停止敏感操作，如远程升级或远程控制。
4. 导出安全日志和最近配置变更。
5. 检查是否存在批量影响。
6. 给出临时缓解措施。
7. 建立复现用例和回归用例。
8. 更新门禁和追踪矩阵。

不要在未取证前清除日志或强制恢复出厂设置。

## 最小落地清单

优先实现：

- 敏感命令权限表。
- 固件镜像 header 和 CRC/hash。
- Bootloader 写保护。
- 写配置范围检查。
- 安全事件日志。
- 失败安全状态。

进一步增强：

- 签名校验。
- 防回滚计数器。
- 远程诊断认证。
- 安全日志远程导出。
- 自动化安全测试。
- 安全风险追踪矩阵。

## 延伸阅读

- `COMMUNICATION_QUALITY_GATE.md`
- `COMMUNICATION_TRACEABILITY.md`
- `COMMUNICATION_OBSERVABILITY.md`
- `FIELD_DIAGNOSTIC_RUNBOOK.md`
- `REGRESSION_TEST_MATRIX.md`
- `bus/uds-deep-dive.md`
- `bus/usb-dfu-deep-dive.md`
